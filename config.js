export const CONFIG = {
    cellSize: 16,  // Increased from 8 to 16 for better visibility
    gridWidth: 25,  // Adjusted to fit the screen better
    gridHeight: 25, // Adjusted to fit the screen better
    treeDensity: 0.4,
    playerSpeed: 2, // Increased from 1 to 2 to compensate for larger cellSize
    hitboxReduction: 2,
    animationSpeed: 5,
    minEntranceExitDistance: 15, // Adjusted for the new grid size
    spriteSize: 32
};