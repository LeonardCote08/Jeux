export const CONFIG = {
    cellSize: 8,
    gridWidth: 38,  // Réduit de moitié par rapport à la version précédente
    gridHeight: 38, // Réduit de moitié par rapport à la version précédente
    treeDensity: 0.4,
    playerSpeed: 1,
    hitboxReduction: 1,
    animationSpeed: 5,
    minEntranceExitDistance: 20, // Ajusté en conséquence
    spriteSize: 32
};